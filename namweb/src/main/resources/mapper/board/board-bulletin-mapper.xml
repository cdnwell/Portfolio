<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.namweb.domain.board.bulletin.mapper.BoardBulletinMapper">

	<select id="selectBoardList" parameterType="int" resultType="board">
		SELECT bno, category, title, writer, post_date as postDate, email,  bview as bView,
			(select count(*) from board_reply br inner join board bi on bi.bno = br.bno) as bReply
		FROM board
		order by bno desc
		LIMIT #{pageStartNo}, 15
	</select>
	
	<select id="selectBoardListCount" resultType="int">
		select count(*) from board
	</select>
	
</mapper>